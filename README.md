# 🚀 Trae 自动化工作流系统

[![GitHub](https://img.shields.io/badge/GitHub-CyangZhou/trae--automation--workflows-blue)](https://github.com/CyangZhou/trae-automation-workflows)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)
[![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)](https://www.python.org/)

> 一套完整的自动化工作流框架，支持动态规划、知识调研、自动修复和工作流市场

---

## 📋 目录

- [功能特性](#功能特性)
- [快速开始](#快速开始)
- [安装方法](#安装方法)
- [使用指南](#使用指南)
- [内置工作流](#内置工作流)
- [工作流编写](#工作流编写)
- [核心原理](#核心原理)
- [目录结构](#目录结构)
- [常见问题](#常见问题)

---

## ✨ 功能特性

| 特性 | 说明 |
|------|------|
| 🎯 **智能触发** | 通过关键词自动识别并执行工作流 |
| 🔄 **动态规划** | 根据任务自动生成执行计划 |
| 📚 **知识调研** | 遇到新技术自动调研官方文档 |
| 🔧 **自动修复** | 执行失败时自动尝试修复 |
| 📦 **工作流市场** | 一键搜索和安装社区工作流 |
| ✅ **强制验证** | 确保代码质量和目标达成 |

---

## 🚀 快速开始

### 方式一：在 Trae 中使用（推荐）

1. **安装到 Trae**
   
   将本仓库克隆到 Trae 的全局目录：
   
   ```bash
   # Windows
   git clone https://github.com/CyangZhou/trae-automation-workflows.git %USERPROFILE%\.trae-cn\workflows
   
   # macOS/Linux
   git clone https://github.com/CyangZhou/trae-automation-workflows.git ~/.trae-cn/workflows
   ```

2. **安装依赖**
   
   ```bash
   pip install pyyaml
   ```

3. **开始使用**
   
   在 Trae 对话中直接说：
   
   ```
   生成提交摘要
   ```
   
   或
   
   ```
   统计项目代码量
   ```

### 方式二：作为独立工具使用

```bash
# 克隆仓库
git clone https://github.com/CyangZhou/trae-automation-workflows.git
cd trae-automation-workflows

# 列出所有工作流
python workflow-runner/workflow_runner.py list

# 执行工作流
python workflow-runner/workflow_runner.py run git-commit-summary
```

---

## 📦 安装方法

### 完整安装（推荐）

```bash
# 1. 克隆到 Trae 全局目录
git clone https://github.com/CyangZhou/trae-automation-workflows.git ~/.trae-cn/trae-automation-workflows

# 2. 创建符号链接（可选，方便管理）
# Windows（以管理员身份运行）
mklink /D "%USERPROFILE%\.trae-cn\workflows" "%USERPROFILE%\.trae-cn\trae-automation-workflows\workflows"

# macOS/Linux
ln -s ~/.trae-cn/trae-automation-workflows/workflows ~/.trae-cn/workflows

# 3. 安装 Python 依赖
pip install pyyaml requests
```

### 最小安装（仅工作流）

```bash
# 只复制工作流文件
mkdir -p ~/.trae-cn/workflows
cp -r trae-automation-workflows/workflows/*.yaml ~/.trae-cn/workflows/
cp -r trae-automation-workflows/workflows/workflow_manager.py ~/.trae-cn/workflows/
```

### 项目级安装

```bash
# 在项目目录下安装
mkdir -p .trae/workflows
cp -r trae-automation-workflows/workflows/*.yaml .trae/workflows/
cp -r trae-automation-workflows/workflows/workflow_manager.py .trae/workflows/
```

---

## 📖 使用指南

### 触发词列表

在 Trae 对话中使用以下关键词：

| 触发词 | 执行的工作流 |
|--------|-------------|
| `开始`、`继续`、`自主执行` | Autonomous Agent |
| `智能工作流`、`智能路由` | 智能工作流推荐 |
| `生成提交摘要`、`周报` | Git 提交摘要 |
| `统计项目`、`代码统计` | 项目统计 |
| `会议纪要` | 会议纪要生成 |
| `代码审查` | 代码审查助手 |
| `发布说明` | Release Notes |
| `站会报告` | 每日站会 |

### 命令行使用

```bash
# 列出所有工作流
python workflow_manager.py list

# 执行工作流
python workflow_manager.py run <workflow-name>

# 带参数执行
python workflow_manager.py run git-commit-summary --var days=7

# 搜索工作流市场
python ../workflow-market/workflow_market.py search "ci"

# 安装工作流
python ../workflow-market/workflow_market.py install <workflow-id>
```

### Python API

```python
from workflow_runner import run_workflow, list_workflows

# 列出所有工作流
workflows = list_workflows()
for wf in workflows:
    print(f"- {wf['name']}: {wf['description']}")

# 执行工作流
result = run_workflow("git-commit-summary")
print(result)

# 带上下文执行
result = run_workflow("project-stats", context={
    "output_dir": "./reports",
    "include_tests": True
})
```

---

## 📁 内置工作流

### 开发工具类

| 工作流 | 触发词 | 功能 |
|--------|--------|------|
| `git-commit-summary` | 提交摘要、周报 | 获取 Git 提交记录生成摘要 |
| `project-stats` | 统计项目、代码统计 | 统计代码量、文件数 |
| `code-review` | 代码审查 | 自动代码质量检查 |
| `create-readme` | 创建README | 自动创建项目 README |
| `dependency-check` | 检查依赖 | 检查依赖更新和安全漏洞 |

### 文档生成类

| 工作流 | 触发词 | 功能 |
|--------|--------|------|
| `meeting-minutes` | 会议纪要 | 生成结构化会议纪要 |
| `release-notes` | 发布说明 | 生成版本发布文档 |
| `api-documentation` | API文档 | 自动生成 API 文档 |
| `daily-standup` | 站会报告 | 生成每日站会报告 |

### 自动化类

| 工作流 | 触发词 | 功能 |
|--------|--------|------|
| `slack-message-classifier` | Slack分类 | Slack 消息智能分类 |
| `pr-review-assistant` | PR审核 | Pull Request 智能审核 |
| `support-ticket-automation` | 工单处理 | 客户支持工单自动处理 |
| `content-publisher` | 内容发布 | 多平台内容自动发布 |
| `sales-lead-nurturing` | 销售线索 | 销售线索智能培育 |

---

## ✏️ 工作流编写

### 基础结构

```yaml
name: 工作流名称
description: 简短描述
version: "1.0.0"
triggers:
  - "触发词1"
  - "触发词2"

steps:
  - id: 1
    name: 步骤名称
    action: run_command | verify | notify | generate_document
    params:
      command: "具体命令"
    on_failure: continue | stop | retry

output:
  files: ["输出文件列表"]
  format: json | markdown | txt
```

### 步骤类型

| 动作类型 | 说明 | 必要参数 |
|---------|------|---------|
| `run_command` | 执行 Shell 命令 | `command` |
| `verify` | 验证结果 | `type`, `path` |
| `notify` | 通知用户 | `message` |
| `generate_document` | 生成文档 | `output`, `content` |
| `search_web` | 联网搜索 | `query` |

---

## 🔧 核心原理

### 七层执行架构

```
用户输入
    ↓
┌─────────────────────────────────────────────────────────────┐
│  第零层：初始化检查层 (Initialization)                        │
│  • 自动创建知识库目录结构                                     │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│  第一层：任务解析层 (Task Parser)                             │
│  • 提取任务核心目标                                           │
│  • 识别技术领域                                               │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│  第二层：知识调研层 (Knowledge Research) ⭐ 核心层             │
│  • 官方文档优先                                               │
│  • 交叉验证                                                   │
│  • 时效性检查                                                 │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│  第三层：工作流匹配层 (Workflow Matcher)                      │
│  • 本地工作流 → GitHub → 教程 → 自动生成                      │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│  第四层：执行层 (Executor)                                    │
│  • 执行具体任务                                               │
│  • 失败自动修复                                               │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│  第五层：验证层 (Validator)                                   │
│  • 代码质量验证                                               │
│  • 目标达成验证                                               │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│  第六层：知识沉淀层 (Knowledge Persistence)                   │
│  • 保存工作流和知识摘要                                       │
└─────────────────────────────────────────────────────────────┘
```

---

## 📂 目录结构

```
trae-automation-workflows/
├── README.md                          # 本文档
│
├── workflow-runner/                   # 工作流执行器
│   ├── SKILL.md                       # Skill 定义
│   └── workflow_runner.py             # 执行器核心代码
│
├── workflow-market/                   # 工作流市场
│   ├── SKILL.md                       # Skill 定义
│   ├── workflow_market.py             # 市场核心代码
│   ├── sources.json                   # 来源配置
│   └── workflow-index.json            # 工作流索引
│
├── autonomous-agent/                  # 自主执行层
│   ├── SKILL.md                       # Skill 定义
│   └── skill.yaml                     # 配置文件
│
├── workflows/                         # 工作流定义
│   ├── workflow_manager.py            # 工作流管理器
│   ├── autonomous_agent.py            # 自主执行引擎
│   ├── git-commit-summary.yaml        # Git提交摘要
│   ├── project-stats.yaml             # 项目统计
│   └── ...                            # 更多工作流
│
└── templates/                         # 文档模板
    ├── meeting-minutes.md             # 会议纪要模板
    └── ...                            # 更多模板
```

---

## ❓ 常见问题

<details>
<summary><b>Q: 工作流执行失败怎么办？</b></summary>

A: 系统会自动尝试修复：
1. 本地修复（最多3次）
2. 联网搜索解决方案
3. 跳过继续执行（静默模式）

可在输出报告中查看详细错误信息。
</details>

<details>
<summary><b>Q: 如何添加新的工作流？</b></summary>

A: 两种方式：
1. 在 `workflows/` 目录创建 YAML 文件
2. 使用 Workflow Market 一键安装

```bash
python workflow_market.py install <workflow-id>
```
</details>

<details>
<summary><b>Q: 如何在 Trae 中使用？</b></summary>

A: 
1. 将工作流复制到 `~/.trae-cn/workflows/` 目录
2. 在对话中使用触发词即可自动执行

```
用户：生成提交摘要
Trae：[自动执行 git-commit-summary 工作流]
```
</details>

---

## 🤝 贡献指南

欢迎贡献工作流和改进建议！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/amazing-workflow`)
3. 提交更改 (`git commit -m 'Add amazing workflow'`)
4. 推送到分支 (`git push origin feature/amazing-workflow`)
5. 创建 Pull Request

---

## 📄 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

---

## 🙏 致谢

- [GitHub Actions](https://github.com/actions) - 工作流灵感来源
- [n8n](https://n8n.io) - 自动化工作流参考
- [OpenHands](https://github.com/All-Hands-AI/OpenHands) - 自主执行架构参考

---

<p align="center">
  Made with ❤️ for Trae AI Assistant
</p>