---
name: workflow-runner
description: 执行 Trae 工作流模板系统 v2.0。支持预定义工作流、自动化重复任务、蜂群模式并行执行。触发词：git-commit-summary、project-stats、swarm-execution 等。
---

# Workflow Runner - 工作流执行器 v2.0

## 功能

自动检测并执行 `.trae/workflows/` 目录下的工作流模板。
🆕 支持蜂群模式并行执行复杂任务。

## 可用工作流

### 1. git-commit-summary
**触发词**: "生成提交摘要", "git summary", "周报"
**功能**: 获取最近Git提交记录并生成摘要文档

### 2. project-stats
**触发词**: "统计项目", "project stats", "代码统计"
**功能**: 统计项目代码量、文件数等信息

### 3. 🆕 swarm-execution
**触发词**: "蜂群", "并行执行", "swarm", "/swarm", "启动蜂群"
**功能**: 智能体蜂群并行执行复杂任务
**特点**:
- 自动任务分解
- DAG 依赖调度
- 多 Worker 并行执行
- 结果自动整合

## 使用方法

### 单工作流执行

```python
from workflow_runner import run_workflow

result = run_workflow("git-commit-summary")
```

### 🆕 蜂群模式执行

```python
from workflow_runner import run_swarm_workflow

result = run_swarm_workflow("开发一个用户认证系统", max_workers=3)
```

### 命令行使用

```bash
# 列出所有工作流
python .trae/skills/workflow-runner/workflow_runner.py list

# 执行单个工作流
python .trae/skills/workflow-runner/workflow_runner.py run --workflow git-commit-summary

# 🆕 启动蜂群模式
python .trae/skills/workflow-runner/workflow_runner.py swarm --task "开发一个用户认证系统" --workers 3
```

## 蜂群模式架构

```
用户请求
    ↓
┌─────────────────────────────────────┐
│      Swarm Orchestrator             │
│  ┌─────────────────────────────┐    │
│  │ 1. 分析任务复杂度            │    │
│  │ 2. 分解为子任务              │    │
│  │ 3. 构建 DAG 依赖图           │    │
│  │ 4. 分发 Worker               │    │
│  └─────────────────────────────┘    │
└─────────────────────────────────────┘
    ↓
┌─────────┐ ┌─────────┐ ┌─────────┐
│Researcher│ │  Coder  │ │ Tester  │ ← 并行执行
└─────────┘ └─────────┘ └─────────┘
    ↓
结果整合 → 最终输出
```

## Worker 类型

| Worker | 职责 | 超时 |
|--------|------|------|
| Researcher | 调研、搜索、知识提取 | 180s |
| Coder | 代码编写、重构、修复 | 600s |
| Tester | 测试、验证、检查 | 300s |
| Writer | 文档、README、API文档 | 180s |
| Reviewer | 代码审查、安全检查 | 180s |

## 触发词映射

| 触发词 | 工作流 |
|--------|--------|
| 提交摘要, git summary, 周报 | git-commit-summary |
| 统计项目, 代码统计 | project-stats |
| 蜂群, 并行执行, swarm, /swarm | swarm-execution |
